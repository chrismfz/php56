Index: ext/standard/iptc.c
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/ext/standard/iptc.c b/ext/standard/iptc.c
--- a/ext/standard/iptc.c	(revision a95a8565e59c1d10f876051c8af6a82e66009ec4)
+++ b/ext/standard/iptc.c	(revision 65c4f426bae61781bbad633efd7b0fcc95afc857)
@@ -215,7 +215,9 @@
 	}
 
 	if (spool < 2) {
-		fstat(fileno(fp), &sb);
+        if (fstat(fileno(fp), &sb) != 0) {
+            RETURN_FALSE;
+        }
 
 		poi = spoolbuf = safe_emalloc(1, (size_t)iptcdata_len + sizeof(psheader) + 1024 + 1, sb.st_size);
 		memset(poi, 0, iptcdata_len + sizeof(psheader) + sb.st_size + 1024 + 1);
