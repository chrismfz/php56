Index: NEWS
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/NEWS b/NEWS
--- a/NEWS	(revision ca1eda2807a3232bb6e5cfab6b6485141128447c)
+++ b/NEWS	(revision b4ce7fa31c6f2ec0a59f1534dc8a07d648e85cad)
@@ -1099,6 +1099,9 @@
   . FIxed bug #70433 (Uninitialized pointer in phar_make_dirstream when zip
     entry filename is "/"). (CVE-2015-7804) (Stas)
 
+- Phar:
+  . Fixed bug #73809 (Phar Zip parse crash - mmap fail). (cmb)
+
 - Phpdbg:
   . Fix phpdbg_break_next() sometimes not breaking. (Bob)
 
Index: ext/phar/tests/bug73809.phpt
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/ext/phar/tests/bug73809.phpt b/ext/phar/tests/bug73809.phpt
new file mode 100644
--- /dev/null	(revision b4ce7fa31c6f2ec0a59f1534dc8a07d648e85cad)
+++ b/ext/phar/tests/bug73809.phpt	(revision b4ce7fa31c6f2ec0a59f1534dc8a07d648e85cad)
@@ -0,0 +1,30 @@
+--TEST--
+Bug #73809 (Phar Zip parse crash - mmap fail)
+--SKIPIF--
+<?php
+if (!extension_loaded('phar')) die('skip phar extension not available');
+if (!extension_loaded('zip')) die('skip zip extension not available');
+?>
+--FILE--
+<?php
+// create the ZIP to be tested
+$zip = new ZipArchive;
+$zip->open(__DIR__ . '/73809.zip', ZipArchive::CREATE);
+$zip->addFromString('73809.txt', 'yada yada');
+$zip->addFromString('.phar/signature.bin', str_repeat('*', 64 * 1024 + 1));
+$zip->setCompressionName('.phar/signature.bin', ZipArchive::CM_STORE);
+var_dump($zip->close());
+
+try {
+    $phar = new PharData(__DIR__ . '/73809.zip');
+} catch (Exception $ex) {
+    echo $ex->getMessage(), PHP_EOL;
+}
+?>
+--CLEAN--
+<?php
+@unlink(__DIR__ . '/73809.zip');
+?>
+--EXPECTF--
+bool(true)
+phar error: signatures larger than 64 KiB are not supported in zip-based phar "%s"
Index: ext/phar/zip.c
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/ext/phar/zip.c b/ext/phar/zip.c
--- a/ext/phar/zip.c	(revision ca1eda2807a3232bb6e5cfab6b6485141128447c)
+++ b/ext/phar/zip.c	(revision b4ce7fa31c6f2ec0a59f1534dc8a07d648e85cad)
@@ -409,9 +409,14 @@
 			php_stream *sigfile;
 			off_t now;
 			char *sig;
-
-			now = php_stream_tell(fp);
+
 			pefree(entry.filename, entry.is_persistent);
+
+            if (entry.uncompressed_filesize > 0x10000) {
+                PHAR_ZIP_FAIL("signatures larger than 64 KiB are not supported");
+            }
+
+            now = php_stream_tell(fp);
 			sigfile = php_stream_fopen_tmpfile();
 			if (!sigfile) {
 				PHAR_ZIP_FAIL("couldn't open temporary file");
